/**
 * @summary recebe uma hora inicial e fina e retorna em qual slot o evento inicia e quantos slots ele ocupa no total
 */
function getSlots(start,end){

    
}

/**
 * recebe um slot inicial e um final e retorna a hora inicial e final do evento
 */
function getTimeSlot(initial,end){
    
}

arrayFunctions = {
    getSlots : getSlots,
    getTimeSlot: getTimeSlot,
}

module.exports = arrayFunctions
